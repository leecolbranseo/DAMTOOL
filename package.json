{
  "name": "dam-tool-concept",
  "version": "0.1.2",
  "description": "Data & Analytics Maturity Assessment — Node concept, modeled on WPRA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "cf:dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
